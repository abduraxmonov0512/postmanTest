{
  "name": "postman_tests",
  "version": "1.0.0",
  "description": "Postman api autotests",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "run-test": "newman run tests/TenderUz.postman_collection.json --reporters htmlextra --reporter-htmlextra-showOnlyFails --reporter-htmlextra-export ./report.html"
  },
  "author": "Rustam Abduraxmonov",
  "dependencies": {
    "newman": "^5.2.0",
    "newman-reporter-htmlextra": "^1.20.1"
  }
}
